: command not found
ubuntu@ip-172-31-0-133:~/algo-trading/deployment$  ss -tuln | grep 8000
tcp   LISTEN 0      2048             0.0.0.0:8000      0.0.0.0:*
ubuntu@ip-172-31-0-133:~/algo-trading/deployment$

ubuntu@ip-172-31-0-133:~/algo-trading/deployment$ sudo journalctl -u algo-trading.service
-- No entries --
ubuntu@ip-172-31-0-133:~/algo-trading/deployment$
server {
    listen 80;
    server_name _;

    location / {
        root /home/ubuntu/algo-trading/frontend;
        index index.html;
        try_files $uri $uri/ /index.html;
    }

    location /api/ {
        proxy_pass http://127.0.0.1:8000/api/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;
    }
}